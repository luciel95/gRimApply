#include <string>
#include <vector>

#define RAMB_KEBOB 12000
#define SODA 2000

using namespace std;

int solution(int n, int k) {
    int answer = 0;
    int forFree = 0;
    
    forFree = n / 10;
    
    answer = (RAMB_KEBOB * n) + (SODA * (k - forFree));
    
    return answer;
}
